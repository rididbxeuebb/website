baseURL = 'https://rididbxeuebb.pages.dev'
languageCode = 'en'
title = 'rididbxeuebb'
disableKinds = ['taxonomy', 'term', 'section']

[markup]
[markup.highlight]
codeFences = true
guessSyntax = true
style = 'dracula'
noClasses = true

[params]
tagline = 'Notes from a developer who dislikes the modern bloated web.'
description = 'A restrained developer blog focused on clear writing, durable markup, and a rejection of unnecessary web complexity.'
